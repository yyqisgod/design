jdk和cglib动态代理对比
1.jdk实现了被代理类接口，cglib继承了被代理对象
2.JDK和cglib都在运行期生成字节码，cglib实现更复杂，生成代理类比jdk动态代理效率低
3.jdk通过反射调用代理方法，cglib通过FastClass机制直接调用，执行效率更高

静态代理和动态代理本质区别
1.静态代理只能手动完成代理操作，如果被代理类新增方法，代理类需要同步增加
2.动态代理采用运行时动态生成代码方式，取消对被代理类扩展限制
3.若动态代理要对目标类的增强逻辑进行扩展，结合策略模式，只需新增策略类便可完成，无须修改代理类代码

代理模式优缺点
    优点：
        1.将代理对象与真实被调用目标对象分离
        2.一定程度降低了耦合性，扩展性好
        3.起到保护目标对象的作用
        4.可以增强目标对象的功能
    缺点：
        1.会增加系统设计类的数量
        2.在客户端和目标对象中增加一个代理对象，会导致请求处理速度变慢
        3.增加系统复杂度